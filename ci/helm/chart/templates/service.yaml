apiVersion: v1
kind: Service
metadata:
  name: {{ include "social-app.fullname" . }}
  labels:
    app: {{ include "social-app.name" . }}
    chart: {{ include "social-app.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
    {{- if .Values.env.envName }}
    env: {{ .Values.env.envName }}
    {{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ include "social-app.name" . }}
    release: {{ .Release.Name }}
