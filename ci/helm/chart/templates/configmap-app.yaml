{{- if .Values.app.config.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ template "social-app.fullname" . }}-app-config
  labels:
    app: {{ template "social-app.name" $ }}
    chart: {{ template "social-app.chart" $ }}
    release: "{{ $.Release.Name }}"
    heritage: "{{ $.Release.Service }}"
data:
  app.conf: |-
    {{ .Values.app.config.configMapData | nindent 4 }}

{{- end -}}
